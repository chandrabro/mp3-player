# Get this from your Vercel project's Storage tab after creating a Blob store:
# https://vercel.com/docs/storage/vercel-blob
# If this is left unset, /api/upload automatically falls back to a local
# mock (data-URL) response so development still works end to end.
BLOB_READ_WRITE_TOKEN=
